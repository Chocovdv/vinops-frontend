---
// src/pages/app/bodega/index.astro
export const prerender = false;

import AppLayout from "../../../layouts/AppLayout.astro";
import BodegaDatos from "../../../components/bodega/BodegaDatos.jsx";
import BodegaMapa from "../../../components/bodega/BodegaMapa.jsx";

const pageTitle = "VinOps – Datos de la bodega";
const activeSection = "bodega";
---

<AppLayout pageTitle={pageTitle} activeSection={activeSection}>
  <section class="container py-3">
    <h1 class="h3 mb-3">Datos de la bodega</h1>

    <!-- Mapa debajo del título -->
    <BodegaMapa client:load />

    <!-- Datos de la bodega como antes -->
    <BodegaDatos client:load />
  </section>
</AppLayout>
