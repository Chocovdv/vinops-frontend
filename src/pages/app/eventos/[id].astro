---
// src/pages/app/eventos/[id].astro
export const prerender = false;

import AppLayout from "../../../layouts/AppLayout.astro";
import EventoDetalle from "../../../components/eventos/EventoDetalle.jsx";

const { id } = Astro.params;
const pageTitle = "VinOps – Detalle evento";
const activeSection = "eventos";
---

<AppLayout pageTitle={pageTitle} activeSection={activeSection}>
  <section class="container py-3">
    <EventoDetalle client:load eventoId={id} />
  </section>
</AppLayout>
