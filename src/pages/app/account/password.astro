---
// src/pages/app/account/password.astro
export const prerender = false;

import AppLayout from "../../../layouts/AppLayout.astro";
import CambiarPassword from "../../../components/account/CambiarPassword.jsx";

const pageTitle = "Cambiar contraseña | VinOps";
const activeSection = "account";
---

<AppLayout pageTitle={pageTitle} activeSection={activeSection}>
  <section class="container py-3">
    <h1 class="h3 mb-3">Cambiar contraseña</h1>
    <CambiarPassword client:load />
  </section>
</AppLayout>
