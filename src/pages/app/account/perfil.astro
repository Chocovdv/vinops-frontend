---
// src/pages/app/account/perfil.astro
export const prerender = false;

import AppLayout from "../../../layouts/AppLayout.astro";
import MiPerfil from "../../../components/account/MiPerfil.jsx";

const pageTitle = "Mi perfil | VinOps";
const activeSection = "account";
---

<AppLayout pageTitle={pageTitle} activeSection={activeSection}>
  <section class="container py-3">
    <h1 class="h3 mb-3">Mi perfil</h1>
    <MiPerfil client:load />
  </section>
</AppLayout>
