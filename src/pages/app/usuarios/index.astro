---
// src/pages/app/usuarios/index.astro
export const prerender = false;

import AppLayout from "../../../layouts/AppLayout.astro";
import UsuariosList from "../../../components/usuarios/UsuariosList.jsx";

const pageTitle = "Usuarios de la bodega | VinOps";
const activeSection = "usuarios";
---

<AppLayout pageTitle={pageTitle} activeSection={activeSection}>
  <section class="container py-3">
    <h1 class="h3 mb-3">Usuarios de la bodega</h1>
    <UsuariosList client:load />
  </section>
</AppLayout>
