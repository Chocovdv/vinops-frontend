---
import PublicRedirectGuard from "../components/layout/PublicRedirectGuard.jsx";

export interface Props {
  title?: string;
}

const { title = "VinOps · La operativa diaria de tu bodega" } = Astro.props;
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>{title}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- BOOTSTRAP CSS (CDN) -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />

    <!-- Estilos propios -->
    <link rel="stylesheet" href="/css/app.css" />
  </head>
  <body class="bg-light">
    <!-- NAVBAR PÚBLICO -->
    <nav class="navbar navbar-expand-lg public-navbar fixed-top">
      <div class="container">
        <!-- Marca simple, sin iconos -->
        <a class="navbar-brand fw-semibold" href="/">
          VinOps
          <span class="text-muted small ms-1">· Gestión de bodegas</span>
        </a>

        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#publicNavbar"
          aria-controls="publicNavbar"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="publicNavbar">
          <ul class="navbar-nav ms-auto me-3 mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="#que-es">Qué es VinOps</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#modulos">Módulos</a>
            </li>
            <!-- 🔹 Nuevo enlace al asistente de registro -->
            <li class="nav-item">
              <a class="nav-link" href="#asistente-registro">Registro</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#multibodega">Multibodega</a>
            </li>
          </ul>

          <div class="d-flex">
            <a href="/login" class="btn btn-primary">
              Iniciar sesión
            </a>
          </div>
        </div>
      </div>
    </nav>

    <!-- CONTENIDO ESPECÍFICO -->
    <main class="public-main">
      <slot />
    </main>

    <!-- FOOTER -->
    <footer class="home-footer py-3 border-top bg-white">
      <div
        class="container d-flex flex-column flex-md-row justify-content-between align-items-center"
      >
        <div class="small">
          &copy; {year} VinOps · TFG 2º DAW
        </div>
        <div class="small mt-2 mt-md-0">
          Aplicación web para gestionar la operativa diaria de una bodega
          pequeña.
        </div>
      </div>
    </footer>

    <!-- BOOTSTRAP JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>

    <PublicRedirectGuard client:load />
  </body>
</html>
